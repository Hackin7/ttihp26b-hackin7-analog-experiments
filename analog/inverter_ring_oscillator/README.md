# Previous analog leaf (stdcell ring)

This directory is the **previous** analog leaf: three `sg13g2_inv_1`
inverters. Tiny Tapeout now binds `ring_oscillator` to
`analog/transistor_ring_oscillator/macro/` instead.

Keep these files as a reference for the stdcell-based ring. Source layout
and schematic live in `layout/` and `schematic/`; exported views are in
`macro/`.
